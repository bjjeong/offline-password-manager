import React from 'react';
import {
    StyleSheet, View,
} from 'react-native';

const Login = () => {
    return (
        <View />
    );
};

const styles = StyleSheet.create({
});

export default Login;
